# Handoff: {TITLE}

> Generated by Opus | Date: {DATE} | For: Codex Implementation

---

## Goal

{한 문장으로 완료의 의미를 정의}

## Background (Why)

{왜 이 작업이 필요한지 - Opus의 분석/리서치 결과 요약}

- **근본 원인**: {원인 분석 결과, 있는 경우}
- **결정 사유**: {이 접근 방식을 선택한 이유}
- **탈락 대안**: {검토했지만 선택하지 않은 방법과 그 이유}

---

## Scope

### Files to Modify
```
{파일경로1}  ← {무엇을 변경}
{파일경로2}  ← {무엇을 변경}
```

### Files to Create
```
{새파일경로1}  ← {목적}
```

### Files NOT to Touch
```
{수정금지파일1}  ← {이유}
```

---

## Function Signatures

```{language}
// {파일경로}
{function_signature_1}
{function_signature_2}
```

---

## Acceptance Criteria

- [ ] AC1: WHEN {조건} THEN {동작} — 검증: `{검증 명령어}`
- [ ] AC2: WHEN {조건} THEN {동작} — 검증: `{검증 명령어}`
- [ ] AC3: {기존 테스트/빌드 통과} — 검증: `{빌드/테스트 명령어}`

---

## Constraints

- **DO NOT**: {금지사항 1}
- **DO NOT**: {금지사항 2}
- **MUST**: {필수 준수사항}

---

## Reference Pattern

기존 코드베이스에서 참고할 패턴:
```
{참조파일}: {어떤 패턴을 따를지}
```

---

## Edge Cases

| 상황 | 예상 동작 |
|------|----------|
| {엣지케이스1} | {처리 방법} |
| {엣지케이스2} | {처리 방법} |

---

## Verification

구현 완료 후 실행할 검증 명령어:

```bash
# 빌드 검증
{빌드 명령어}

# 테스트 실행
{테스트 명령어}

# 기능 검증
{기능 테스트 명령어 또는 curl 예시}
```

### Expected Output
```
{예상 출력}
```

---

## Notes for Codex

- 이 명세서는 Opus가 {분석/리서치/설계} 단계를 완료한 결과입니다
- 구현 중 명세서와 다른 접근이 필요하면, 해당 부분을 주석으로 남기고 진행해주세요
- 완료 후 git commit 메시지에 `handoff: {feature-slug}` 접두사를 사용해주세요
